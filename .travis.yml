language: python

python:
  - "2.7"

install:
  - pip install .

script:
  # pytest
  - pytest
  # script docs
  - UniFastaFormat.py -h
  - mfe_index_db.py -h
  - MFE_primer.py -h
  # example
  - UniFastaFormat.py -i test/data/test.rna
  - faToTwoBit test/data/test.rna.unifasta test/data/test.rna.2bit
  - mfe_index_db.py -f test/data/test.rna.unifasta -k 9